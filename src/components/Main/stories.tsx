import { Meta, StoryObj } from "@storybook/react";
import Main from ".";

const meta: Meta<typeof Main> = {
  title: "Main",
  component: Main,
};

export default meta;

export const Default: StoryObj = {};
